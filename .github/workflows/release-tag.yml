name: Release tags disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  policy:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm repository publication policy
        run: echo "GitHub Releases and automatic Git tags are disabled; publish through a reviewed branch and pull request."
